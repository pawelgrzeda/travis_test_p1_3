  os: linux
  dist: focal
  arch: amd64
  group: dev
  virt: lxd
  jobs:
    include:
      - language: python
        script:
          - python hello.py

      - language: smalltalk
        before_install:
          - "sudo apt-get install libpulse0"
        smalltalk_vm:
          - Squeak-5.0
        arch:
          - arm64-graviton2
        smalltalk:
          - Squeak64-trunk
