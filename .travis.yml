  os: linux
  dist: focal
  arch: arm64-graviton2

  jobs:
    include:
      - language: python
        script:
          - python hello.py

      - language: smalltalk
        before_install:
          - "sudo apt-get install libpulse0"
        group: edge
        virt: lxd
        smalltalk_vm:
          - Squeak-5.0
        smalltalk:
          - Squeak64-trunk
      - language: smalltalk
        before_install:
          - "sudo apt-get install libpulse0"
        smalltalk_vm:
          - Squeak-5.0
        smalltalk:
          - Squeak64-trunk
